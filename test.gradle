task(testgrooovy).doLast {
    def a=1//使用def来声明变量，后面分号可以省略
    /**
     * 语句后面分号可以省略
     * 变量类型与方法返回值也可以省略
     * 方法调用时，括号也可以省略
     * 甚至方法返回的return也可以省略
     */
    println 'hello groovy'
}
def hello(){
    println('hello')
    1 //返回时 return 省略
}

def helloGroovy(msg){
    print msg
    return 1
}
//Groovy 的闭包可以作为方法的参数和返回值，亦可以作为一个变量而存在
//省略了闭包参数类型
def test= { a,b -> println("a=${a},b=${b},i am a closure")}

def computer = { a,b -> a+b}//这里省略了return

/*
Groovy 加强了Java中的list，map，set
 */
def emptyList = []//声明一个空集合
def testList = [100,"a","b"]
println testList[0]
testList << 200//左移表示向集合中添加一个元素
println testList.size()

outputs:
100
4

//Map 也有左移
def emptyMap = [:]//声明一个空的map集合
def testMap = ["a":1,"name":"yif","address":"hangzhou"]
testMap.name = "ztz"
println testMap.a
println testMap.name

outputs:
1
ztz
