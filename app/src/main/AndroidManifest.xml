<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zhangtianzhu.kotlindemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.DatePickerActivity"
            android:launchMode="singleTask"
            android:screenOrientation="sensorLandscape"/>
        <activity
            android:name=".ui.JumpViewSlef"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.RecyclerViewActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.ListViewActivity" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.TestActivity" />
        <activity android:name=".students.activity.ReadingActivity" />
        <activity android:name=".students.activity.ReadingDetailActivity" />
        <activity android:name=".students.activity.IntensiveReadActivity" />
        <activity android:name=".ui.AnnotationActivity" />
        <activity
            android:name=".ui.AnimatorActivity"
            android:configChanges="screenSize|orientation|keyboardHidden"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.AnimatorActivityTwo"
            android:launchMode="singleTask" />
        <activity android:name=".ui.CalendarActivity" />
    </application>

</manifest>